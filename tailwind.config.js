/* Дизайн-система FluentFox для складання css/style.css.

   Раніше конфіг жив у трьох місцях: інлайном у index.html, урізаною копією в
   кожній статті блогу і в js/tw.js для лендингів. Три копії одного словника
   токенів розходяться після першої ж правки — тут вони зведені в одну.

   Версія Tailwind навмисно 3.x: саме її віддавав cdn.tailwindcss.com, і саме
   під її дефолти написана вся верстка. У 4.x інший формат конфігу й інші
   базові значення — перехід змінив би вигляд сторінок мовчки.

   Складання:  npm run css      (або npm run css:watch під час правок)
*/
module.exports = {
  content: [
    './index.html',
    './404.html',
    './*.html',
    './blog/*.html',
  ],
  theme: {
    extend: {
      // ── Брейкпоінти ────────────────────────────────────────────────────
      screens: {
        // Повне десктопне меню (посилання + два телефони + мова + CTA) вміщається
        // лише звідси. md:768 і lg:1024 замалі — перевірено емпірично, між 768 і
        // 1099 меню переносилося на два рядки.
        nav: '1100px',
      },

      // ── Кольори ────────────────────────────────────────────────────────
      colors: {
        fox: {
          50:  '#FFF5EF',
          100: '#FFE8D6',
          200: '#FFCAAA',
          300: '#FFA07A',
          400: '#FF7A45',
          500: '#FF6B35',
          600: '#E54E1A',
          700: '#BF3C0F',
        },
        violet: {
          50:  '#F4F0FF',
          100: '#EAE2FF',
          500: '#7C3AED',
          600: '#6D28D9',
        },
        gray: {
          950: '#030712',
        },
        // Темно-червоний. Живе лише під банером «Готуємо до міжнародних іспитів»:
        // автобус і Біг-Бен на ньому — теплі помаранчево-червоні, і на
        // помаранчевому градієнті fox-500 → fox-700 їхні силуети зливалися з
        // фоном. Темніша й глибша червона підкладка дає їм контраст сама, без
        // німбів і рантів навколо картинок.
        brick: {
          600: '#9B1C1C',
          700: '#7A1414',
          800: '#5C0D0D',
        },
        british: '#C8102E',   // червоний прапора, лише смуга London
        cream: '#FFFBF7',
        gold: {
          DEFAULT: '#D4AF37',
          light:   '#F5E88A',
          dark:    '#B8960C',
        },
      },

      // ── Типографіка ────────────────────────────────────────────────────
      fontFamily: {
        sans: ['Nunito', 'sans-serif'],
      },
      fontWeight: {
        medium:   '500',
        semibold: '600',
        bold:     '700',
        black:    '900',
      },
      fontSize: {
        // Єдиний розмір поза шкалою Tailwind: марки іспитів і дрібний текст
        // сертифіката. Решта десяти кроків — дефолтні, вони збігаються зі шкалою
        // з CLAUDE.md один в один.
        '2xs': ['0.6875rem', { lineHeight: '1.5' }],
      },

      // ── Форма ──────────────────────────────────────────────────────────
      borderRadius: {
        '4xl': '2rem',   // hero-блоби й модалки
      },

      // ── Тіні ───────────────────────────────────────────────────────────
      boxShadow: {
        'fox-sm': '0 4px 14px rgba(255,107,53,0.12)',
        'fox':    '0 8px 24px rgba(255,107,53,0.18)',
        'fox-lg': '0 20px 40px rgba(255,107,53,0.22)',
        'fox-xl': '0 28px 56px rgba(255,107,53,0.30)',
      },

      // ── Іменовані градієнти ────────────────────────────────────────────
      backgroundImage: {
        'hero':     'linear-gradient(140deg,#FFF5EF 0%,#FFF9F5 50%,#F4F0FF 100%)',
        'fox-grad': 'linear-gradient(135deg,#FF6B35 0%,#E54E1A 100%)',
        'fox-step': 'linear-gradient(135deg,#FF6B35,#7C3AED)',
      },
    },
  },

  // Класи, які збираються в рантайме й тому не трапляються в HTML як текст.
  // Зараз таких немає — усі Alpine-вирази (`:class="a ? 'bg-fox-500' : 'bg-gray-100'"`)
  // містять цілі рядки, і сканер знаходить їх як звичайний текст. Список
  // лишається порожнім свідомо: якщо колись з'явиться клас, зібраний з шматків,
  // його треба буде дописати сюди — інакше він просто зникне зі стилів.
  safelist: [],

  plugins: [],
};
