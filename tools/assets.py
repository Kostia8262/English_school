# -*- coding: utf-8 -*-
"""Одна версія статики на всі три генератори.

Навіщо. Версія в `?v=` жила у трьох місцях: `ASSET_VERSION` у tools/home,
`CSS_VERSION` у tools/landing і просто числом усередині tools/blog/template.html.
Файл `css/style.css` при цьому один. Варто було підняти версію в одному
генераторі й забути в іншому — і відвідувач, який перейшов із головної на
посадкову, качав той самий файл удруге: кеш у нього річний і `immutable`,
а ключ інший.

Тепер значення одне, і піднімати його треба в одному місці — тут. Воно
версіонує все, що лежить у `/css` і `/js`: складання CSS, js/lang.js, js/home.js.

Сторінки, які не збираються генератором (юридичні, 404/500/503, лістинг блогу),
правляться руками, тому tools/audit/assets.py звіряє кожен `?v=` у репозиторії
з цим рядком і валить аудит на розбіжності.
"""
from __future__ import unicode_literals

ASSET_VERSION = "20260907d"
